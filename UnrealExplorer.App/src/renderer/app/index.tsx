import * as React from 'react';
import MessageToken from '../../common/bridge/message_token';

export function App() {
    return (
        <h3>hello world 😳{JSON.stringify(AppBridge)}</h3>
    )
}